# Product

## Register

product

## Users

运营者每天登录或导入购入的已有 ChatGPT 账号，补充多个 Codex2API 实例，查看失效状态、账号资料、美元产出、剩余额度及购号回本。桌面用于批量比较和操作，手机用于查看状态和处理异常。

## Product Purpose

作为 Codex2API 号池运营中控台，统一补号、账号资产、实例状态与调度、任务及必要设置。只保留 ChatGPT 和 Codex2API，不创建新账号。共享账号按实例使用趋势调度，先保障每套基本可用，再分配余量。购号成本按整批总价分摊，售价按实例配置，历史产出和成本跨节点连续保留。

## Brand Personality

克制、可靠、直接。界面优先传达状态和操作结果，不使用营销化表达或无关装饰。

## Anti-references

避免只适配宽屏的固定横向布局、无法换行的数据行、过度装饰的仪表盘卡片，以及在移动端隐藏关键功能的简化方案。

## Design Principles

1. 状态优先：任务结果、错误、额度和下一步操作必须先于内部技术标识。
2. 同一信息架构：桌面与移动端使用相同概念，只调整排列和密度。
3. 数据可读：数字与短标签保持完整，不允许被挤压成逐字竖排或溢出容器。
4. 操作可靠：触控目标清晰，查看、删除、保存等操作在手机上仍可直接完成。
5. 渐进披露：列表展示摘要，详情和日志按需打开。
6. 延续现有字体与字号规范；重做信息架构、布局和交互，通过对齐、间距与分组降低阅读负担。
7. 真实数据：区分当前页和全局、额度和已计费、实时值和估算；现有接口尚未实现的业务指标不伪造。

## Accessibility & Inclusion

保持 Ant Design 的键盘、焦点和语义能力；正文与状态文本维持可读对比度；不依赖颜色单独表达状态；移动端触控按钮保留足够高度和间距。
