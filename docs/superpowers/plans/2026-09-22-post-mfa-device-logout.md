# 实施计划

1. 为 MFA 管理器添加退出所有设备的协议测试；为登录引擎添加两种登录阶段的顺序、失败、会话隔离测试，先确认测试暴露缺失行为。
2. 在 MFA 保存成功之后调用已核对的服务端接口。清理旧手机授权缓存，RT 流程强制全新 OAuth 登录，AT 流程强制全新 Web 登录并使用其所有返回凭据。
3. 补齐强制绑定 MFA 的同等处理，保留 Free 删除和已激活 MFA 恢复机制。更新界面流程说明。
4. 执行后端相关回归、前端测试和构建，并独立审查修改。
5. 提交并同步仓库。暂停调度、检查活动任务、备份数据库，发布到服务器，恢复调度并验证线上健康。
