# 中控台界面规范

## 场景与方向

运营者在桌面浏览器连续检查多个实例和一批批账号，需要快速比较数据并进入补号或异常处理。采用克制的运营工作台布局，保留明暗主题；中性表面承载数据，现有靛蓝只标识主要操作与当前位置。页面以分区、表格、工具栏、列表和按需打开的详情组织，避免层层卡片、巨大标题、装饰渐变和无意义动画。

## 必须沿用的排版

- 数字字体资源：`frontend/public/fonts/TCloudNumberVF.ttf`，名称 `TCloudNumber`，`font-display: swap`。
- 界面字体栈：`TCloudNumber, -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', Arial, sans-serif`。
- 日志、代码、Token 与需字符对齐的技术内容沿用 `--font-family-mono`。
- Ant Design 正文和控件保持现有默认 14px；辅助文字12px；既有紧凑元数据10/11px保留其用途，不用来压缩正文。
- 页面标题采用当前28px规范，区块标题16px，原组件已有14/16px标题保持角色一致。统计数字沿用当前组件既有24/28px档位。
- 不新增字体、不全局缩放字号、不用fluid/clamp字体。响应式只调整布局与留白。
- 以 `frontend/src/typography.test.ts` 与浏览器 computed style 核验字体；现有字体资源、主题字体配置和技术等宽规则继续生效。

## 信息架构

主要入口：运营总览、账号池、补充账号、实例管理、号池调度、任务记录。设置位于辅助导航，包含登录和恢复、Bark/邮件、连接与必要运行配置；代理和接码资源作为辅助入口。旧多平台和从零注册入口退出主工作流。

补充账号集中呈现已有账号登录、JSON/凭据导入、邮箱资料导入。表单复用现有真实业务链路，展示任务进度、结果、失败原因和下一步操作，并录入本批购号总成本。账号池按用户最新要求使用独立卡片，容器宽度足够时三列、适中时两列、手机单列；同排等高、底部操作对齐，保持筛选、分页、成本编辑、状态、额度、资料及批量操作。

运营总览使用紧凑的今日/累计经营指标带、回本进度、账号状态、近7日产出趋势、实例经营比较、购号批次和待处理信息。图表日期标签固定12px，缺失日显示断点；金额来源为真实接口，未知价格、金额或日期明确显示待补齐。

## 组件与布局契约

- 共用 `ConsolePageHeader`：`title`, `description?`, `actions?`, `children?`，输出页面标题和操作区域。
- 页面容器 `.console-page`，头部 `.console-page-header`，工具栏 `.console-toolbar`，平面分区 `.console-panel`，分区标题 `.console-section-heading`。
- 全局颜色变量：`--console-bg`, `--console-surface`, `--console-surface-alt`, `--console-border`, `--console-text`, `--console-muted`, `--console-accent`, `--console-accent-soft`。
- 间距以4/8px为基准：控件8，组内12/16，区块24/32；面板以1px边界区分，不套嵌卡片。
- 桌面固定侧栏、清晰的内容顶部导航，移动端用可关闭的抽屉导航；主内容独立收缩，宽表格只在自身区域横向滚动。
- 仅为交互状态使用150–200ms过渡；支持reduced motion，不延迟内容出现。

## 交付检查

验证1440、1024、390px视口；明暗主题均检查账号、实例、补号、任务及设置。检查中文、邮箱、金额、长错误、空数据、加载、请求失败、弹窗、键盘焦点与触控可达性。数据未知显示明确状态，禁止用演示数字充当真实运营结果。
